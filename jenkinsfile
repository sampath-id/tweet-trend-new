pipeline {
    agent any

    stages {
        stage('clone-code') {
            steps {
                git branch: 'main', url: 'https://github.com/sampath-id/tweet-trend-new.git'
            }
        }
    }
}

