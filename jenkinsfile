pipeline {
    agent {
        node {
            label 'maven'
        }
    }
environment {
    PATH = "/user/share/apache-maven-3.6.3/bin:$PATH"
}
    stages {
        stage("build"){
            steps {
               sh 'mvn clen deploy'
            }
        }
    }
}
                      
